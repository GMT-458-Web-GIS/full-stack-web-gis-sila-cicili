@model IEnumerable<LibrarySystem.Models.Loan>

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>🔄 Ödünç Takip Listesi</h2>
        <a asp-action="Create" class="btn btn-success btn-lg">Kitap Ödünç Ver +</a>
    </div>

    <table class="table table-hover table-bordered shadow-sm">
        <thead class="table-dark">
            <tr>
                <th>ID</th>
                <th>Kitap Adı</th>
                <th>Alan Üye</th>
                <th>Veriliş Tarihi</th>
                <th>Son Teslim</th>
                <th>Durum</th>
                <th>İşlem</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.LoanId</td>
                    
                    <td>@item.Book?.Title</td>
                    <td>@item.User?.Username</td>
                    
                    <td>@item.BorrowDate?.ToString()</td>
                    
                    <td>
                        @{
                            // BURASI KRİTİK DÜZELTME:
                            // Şimdiki zamanı "Sadece Tarih" formatına çeviriyoruz.
                            var bugun = DateOnly.FromDateTime(DateTime.Now);
                        }

                        @if (item.DueDate < bugun)
                        {
                             <span class="text-danger fw-bold">@item.DueDate (Gecikmiş!)</span>
                        }
                        else
                        {
                             <span class="text-success">@item.DueDate</span>
                        }
                    </td>
                    
                    <td>
                        <span class="badge bg-warning text-dark">@item.Status</span>
                    </td>
                    
                    <td>
                        <a asp-action="Delete" asp-route-id="@item.LoanId" class="btn btn-sm btn-outline-primary" onclick="return confirm('Kitap iade alındı mı?');">✅ İade Al</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>