@model IEnumerable<LibrarySystem.Models.User>

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>👥 Üye Listesi</h2>
        <a asp-action="Create" class="btn btn-primary btn-lg">Yeni Üye Ekle +</a>
    </div>

    <table class="table table-hover table-bordered shadow-sm">
        <thead class="table-dark">
            <tr>
                <th>ID</th>
                <th>Ad Soyad</th>
                <th>Kullanıcı Adı</th>
                <th>Email</th>
                <th>Rol</th>
                <th>İşlemler</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var user in Model)
            {
                <tr>
                    <td>@user.UserId</td>
                    <td>@user.FirstName @user.LastName</td>
                    <td>@user.Username</td>
                    <td>@user.Email</td>
                    <td>
                        @* 👇 ROL KONTROLÜ BURADA GÜNCELLENDİ 👇 *@
                        @if(user.Role == "admin") 
                        {
                            <span class="badge bg-danger">Yönetici</span>
                        } 
                        else if (user.Role == "Academic") 
                        {
                            <span class="badge bg-warning text-dark">Akademisyen</span>
                        }
                        else 
                        {
                            <span class="badge bg-info text-dark">Öğrenci</span>
                        }
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@user.UserId" class="btn btn-sm btn-warning">Düzenle</a>
                        <a asp-action="Delete" asp-route-id="@user.UserId" class="btn btn-sm btn-danger" onclick="return confirm('Silmek istediğine emin misin?');">Sil</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>